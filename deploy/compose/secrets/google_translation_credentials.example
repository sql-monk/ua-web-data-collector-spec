OPERATOR-SUPPLIED: credential провайдера перекладу (WP-04) вписує оператор; init-secrets.sh
його НЕ генерує, а створює порожній файл (переклад вимкнено: COLLECTOR_TRANSLATION_PROVIDER=disabled).
Формат і ротація — deploy/compose/README.md, розділ «Секрет провайдера перекладу»; приклад не копіюється.
