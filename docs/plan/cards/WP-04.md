# Картка WP-04 — Translation core

| Поле | Значення |
|---|---|
| Owner | wp-implementer (єдиний owner `src/collector/translation/**` і translation golden corpus; адаптери WP-06x лише додають власні 30-зразкові QA-звіти) |
| Branch | три послідовні PR: `wp/04-1-segmenter-tm`, `wp/04-2-provider-budget`, `wp/04-3-golden-corpus` (кожен — від свіжого `main` після merge попереднього) |
| Worktree | `.worktrees/wp-04` |
| Залежить від | **PR1 — стартує одразу (U-3):** WP-00 PR1–PR4 `merged`, WP-01C PR1 `merged 1f2fbc8` — ready. **PR2 — чекає:** WP-04 PR1; WP-01C PR2 (`NewsTranslation`, `TranslationStatus`, `NewsVersionCreatedEvent`); WP-01A PR3a (defer без спалювання спроби) і PR3b (`news_*`, `translation_segments`, бюджет, GRANT); WP-01D PR1c (`deferred`, lazy registry, `HandlerContext`); WP-00 PR5 (секрет і env провайдера); WP-02 PR2 (`collector.storage` для translated artifact refs — або винесення в PR2b, див. Acceptance PR2); `httpx` у runtime-залежностях — WP-02 PR1. **PR3 — чекає:** PR1 + PR2 `merged`. Усі передумови `pending` на 2026-09-24 (розділ «Передумови») |
| Розблоковує | WP-05 (news SDK викликає planner перекладу), WP-06A–G (переклади по мовах + 30 зразків), WP-11A (translations endpoints), WP-12 (translation lag/cost alerts, capacity class Translation) |
| Розмір | L → три PR одного owner, кожен ≤ ~800 рядків продуктивного коду (fixtures corpus не рахуються) |
| Розділи ТЗ | §1 п.5, §5.4, §5.5 (`content_access`), §8 (lingua/fastText, Google Cloud Translation Advanced, NLLB/Marian — лише як майбутній provider), §9.1 (`news_translations`, `translation_segments`), §9.3 п.7, §10 кроки 11–12, §12.1 (translation gates), §12.2 (мова: metadata → `lang` → classifier), §12.3 (rubric), §13 (роль `collector_translation`, секрети провайдера, prompt-like текст у новинах), §14.1 (translation-метрики), §15 (окрема queue, character budget, пріоритет), §15.1 (клас Translation), §16.1 п.1 і п.7, §16.3 (останні два пункти), §18, §20 Q-008/Q-009, Додаток C «Оригінал + переклад», FR-016, FR-017 |
| Рівні тестів §16.1 | 1 Unit (segmentation/reassembly, language detection, TM key, budget, preservation), 7 Translation QA (golden corpus 16 мов + додаткові пари U-2, preservation, glossary, regression score), 3 Integration (PR2: TM/budget/handler на PostgreSQL під LOGIN-роллю `collector_translation`) |
| Регресії REVIEW.md | R-03 (pipeline перекладу: queue, Google adapter, fallback-інтерфейс, TM, glossary, immutable versions, lineage), R-04 (оригінал не перезаписується; ключ = article version + provider/model + glossary), R-05 (golden corpus, preservation, 30 human-reviewed/мову, rubric ≥ 1.8/2), R-08 (`uk` → `not_required`; змішані сторінки — по сегментах), R-09 (character budget, TM, пріоритет title/lead → body, backfill не витісняє свіжі), R-20 (body nullable; відсутній body не генерується) |
| Q-питання | Q-008 (default: character budget конфігурується; backfill paused без ліміту) і Q-009 (default: лише змінені сегменти, потім збирається повна version) — обидва реалізуються як конфігуровані defaults і фіксуються ADR на етапі 5; Q-006 не стосується |

## Scope

Ядро перекладу новин українською (§10 кроки 11–12): HTML-безпечний segmenter і reassembly, визначення мови по сегментах, translation memory з ключем FR-017, versioned glossary, маскування і перевірка preservation (числа, дати, URL, e-mail, імена/терміни glossary), provider interface з Google Cloud Translation Advanced adapter (online), character budget із пріоритетами title/lead → body нової статті → backfill, `TranslationHandler` для ролі `translation` runtime-у WP-01D, збирання immutable translation version з TM, метрики-лічильники §14.1 і golden multilingual corpus для 16 вихідних мов із offline QA-прогоном.

Модель мов (з §1 п.2, §8 і `docs/research/**`, файли research не змінюються): 19 країн; 16 вихідних мов `de, fr, en, lt, lv, et, pl, hu, ro, cs, sk, sl, hr, it, es, nl`; `uk` — цільова і водночас мова UA-джерел (`not_required`). Мови поза 16 (`ru`, `ca` тощо, наявні в Baltic/ES/RO джерелах) також перекладаються з тими самими gates — рішення користувача U-2 (див. «Рішення оркестратора», О-5). Мова визначається по статті й по сегменту, а не за країною: BE дає `nl`/`fr`/`de` (і `en` у Brussels Times), RO-джерела мають HU/EN секції, LSM — кілька мовних sitemap.

## Out of scope

- Виділення тексту з HTML сторінки, parser order, `content_access` конкретного item — WP-05/WP-06x (WP-04 отримує вже cleaned HTML/текст і `content_access`).
- Схема PostgreSQL, міграції, GRANT — WP-01A (передумова PR3a/PR3b); Pydantic-контракти і JSON Schema — WP-01C (передумова PR2); спільний S3/artifact-store клієнт — WP-02 (`collector.storage`).
- Worker runtime (claim/lease/drain), compose-сервіс `translation-worker` — WP-01D; секрети в `deploy/**` — WP-00.
- Read API перекладів — WP-11A; експорт метрик, алерти, capacity snapshot — WP-12.
- Локальний NLLB/Marian provider: лише інтерфейс, який його допускає; реалізація — після benchmark і ADR (§8).
- Google Cloud **batch** translation (потребує GCS bucket і окремої інфраструктури) — відкладено, out of scope v1 (рішення оркестратора О-6).
- Redis TM hot cache (§8, optional) — не в MVP; PostgreSQL — source of truth.
- Human QA 30 перекладів на мову — виконують люди поза агентами (§7 плану, рівень 7); WP-04 дає інструмент, шаблон rubric і формат `translation_quality_result`.
- Живі виклики Google API у тестах і CI; будь-який live-прогін — лише з дозволу користувача (§11 плану, п. (г) — зовнішні витрати).

## Input / output contracts

- **Вхід (PR2):** outbox-подія `news.version_created` WP-01A PR3 і рядок `news_article_versions` — `article_version_id`, `original_language` (+ source-declared `lang`/`source_locale_raw` із `SourceTime`, `src/collector/contracts/temporal.py`), `content_access` (`ContentAccess`, `src/collector/contracts/enums.py`), `content_hash`, artifact refs на title/lead/cleaned body, ознака backfill. Точний payload — контракт `NewsVersionCreatedEvent` (WP-01C PR2 п.5); рядок і outbox — WP-01A PR3b п.1.
- **Ідемпотентність job-и перекладу:** `translation_idempotency_key(article_version_id, target_language, provider, model_version, glossary_version)` — уже є в `src/collector/contracts/identity.py` (§9.3 п.7); WP-04 використовує саме цю функцію, власної не створює.
- **Вихід:** immutable translation version (`news_translations`: title/lead/body переклад або artifact ref, provider, model/version, glossary version, source content hash, status, quality flags, character count/cost) + рядки `translation_segments` (TM). Контракт `NewsTranslation` і enum статусу в `collector.contracts` **відсутні** — створюються у WP-01C PR2 п.4 (§5.5: агенти не створюють власних взаємозамінних enum).
- **Worker contract:** `TaskHandler`/`Task`/`TaskResult`/`HANDLER_FACTORIES` з `src/collector/workers/handlers.py`; роль `WorkerRole.TRANSLATION` → `collector_translation` (`src/collector/workers/roles.py`), DSN `postgres_dsn_translation` уже змонтовано сервісу `translation-worker` у `docker-compose.yml` (мережі `backend` + `provider-egress`).

## Fixtures

- Існуючих translation-fixtures немає (`tests/fixtures/` містить лише `contracts/`) — усе створюється в цьому WP.
- `tests/fixtures/translation/html/**` (PR1): cleaned HTML із вкладеною розміткою (inline `a/em/strong/br`, списки, цитати, таблиця, `figure/figcaption`, `code`), змішані мови, порожні/`metadata_only` випадки. Авторські синтетичні тексти; копіювати фрагменти реальних статей заборонено (авторське право).
- `tests/fixtures/translation/provider/**` (PR2): записані **форми** відповідей Google v3 `translateText` (успіх, 400, 403, 429 з `Retry-After`, 5xx, обрізана/невалідна відповідь), складені вручну за публічною документацією формату; жодних ключів, токенів, project ID реального проєкту.
- `tests/fixtures/translation/golden/<lang>/**` + `provenance.yaml` (PR3) — див. PR3.

## Owned files

`src/collector/translation/**`, `tests/unit/translation/**`, `tests/integration/translation/**`, `tests/fixtures/translation/**`, `pyproject.toml` + `uv.lock` (лише власні залежності WP-04, алфавітно, з коментарем-власником за зразком наявних: runtime `lingua-language-detector`, `lxml`, `httpx`, `google-auth`; остаточний вибір бібліотек — PR1/PR2 з обґрунтуванням у звіті; `httpx` уже додає WP-02 PR1 — WP-04 лише використовує, без дубля), `docs/plan/reports/WP-04/**`, `docs/plan/deps/WP-04-to-*.md`.

Етап 5 (docs-writer): `docs/translation-qa.md`, `src/collector/translation/README.md`, ADR у `docs/decisions/` (наступні вільні номери на момент етапу 5).

Forbidden: `src/collector/contracts/**`, `schemas/**` (→ WP-01C), `migrations/**`, `src/collector/persistence/**` (→ WP-01A/WP-01B), `src/collector/workers/**` (→ WP-01D; handler WP-04 живе в `collector.translation`, у `workers` не кладеться), `docker-compose.yml`, `deploy/**`, `.github/**` (→ WP-00/WP-01D), `src/collector/fetch/**` (→ WP-02), `src/collector/normalization/**`, `src/collector/adapters/**` (→ WP-05/WP-06x), `src/collector/telemetry/**` (→ WP-12), `src/collector/api/**` (→ WP-11A), `src/collector/cli.py` (QA-runner запускається як `python -m collector.translation.qa`, без нової CLI-команди), `src/collector/storage/**` (→ WP-02, лише споживання), `tests/unit/test_foundation_config.py` (→ WP-02 PR1, рішення оркестратора), `docs/research/**`.

## Передумови (рішення оркестратора, 2026-09-24)

Dependency-запити чернетки оформлено оркестратором як розділи карток власників; окремі
`docs/plan/deps/WP-04-to-*.md` для цих пунктів не подаються. До merge передумов PR2 не
стартує; PR1 від них не залежить.

| Було в чернетці | Де тепер | Потрібно для |
|---|---|---|
| п.1 `WP-04-to-WP-01C` (`TranslationStatus`, `TranslationQualityFlag`, `NewsTranslation`, snapshot; `body_*` nullable) | `WP-01C.md` → PR2 `wp/01c-2-payload-news-contracts`, п.4; TM key — не контракт (п.6, О-2) | PR2 |
| п.2а `translation_segments` під TM key | `WP-01A.md` → PR3b `wp/01a-3b-news-translation`, п.3 | PR2 |
| п.2б таблиця бюджету з reserve/commit/release | `WP-01A.md` → PR3b, п.5 (О-1) | PR2 |
| п.2в `queue.release(..., not_before=)` / defer без інкременту `attempt` | `WP-01A.md` → PR3a `wp/01a-3a-queue-outbox-preflight`, п.1 | PR2 |
| п.2г GRANT `collector_translation` | `WP-01A.md` → PR3b, п.7 | PR2 |
| п.2ґ payload `news.version_created` | `WP-01C.md` → PR2, п.5 (контракт); `WP-01A.md` → PR3b, п.1 (outbox у транзакції) | PR2 |
| п.2д хто ставить translation jobs | `WP-01A.md` → PR3b, п.1–2 (О-3: у транзакції `upsert_article_version`, GRANT ролі викликача `collector_parser`) | PR2 (planner), WP-05 (виклик) |
| п.3а `TaskResult.deferred(until, error_code)` | `WP-01D.md` → PR1c `wp/01d-1c-handler-plumbing`, п.1 | PR2 |
| п.3б імпорт доменного модуля для `HANDLER_FACTORIES` | `WP-01D.md` → PR1c, п.3 (`TRANSLATION → collector.translation.handler`) і п.4 (`HandlerContext`) | PR2 |
| п.3в env провайдера в `translation-worker`; п.4 секрет `google_translation_credentials` | `WP-00.md` → PR5 `wp/00-5-object-store-secrets`, п.3–4 | PR2 |
| п.5 клієнт читання cleaned artifact-ів і upload-claim helper; `httpx` у runtime-залежностях | WP-02 PR2 — спільний `collector.storage` (owner WP-02, рішення оркестратора O-3 у `WP-02.md`): `get(key, expected_sha256)`, `upload`; `httpx` — WP-02 PR1 (виняток оркестратора в `test_foundation_config.py`) | PR2 (або PR2b, див. Acceptance PR2) |
| п.6 інформаційний до WP-05 (формат cleaned HTML, source-declared мова, виклик `plan_translation_jobs`) | переноситься в DoR картки WP-05 (ще не створена) | — |

## Рішення оркестратора, 2026-09-24

- **О-1 — бюджет: нова таблиця у WP-01A PR3** (конкретно PR3b, варіант (а)):
  `translation_budget_periods` з row-lock reserve/commit/release за патерном
  `origin_rate_buckets`. Порт `BudgetLedger` — над репозиторієм WP-01A; тест паралельних
  `reserve` — integration на PostgreSQL (варіант (б) більше не розглядається).
- **О-2 — функція TM key живе в `src/collector/translation/**`** (`collector.translation.memory`,
  owner WP-04); колонка WP-01A зберігає готовий hex; схема ключа в контрактах WP-01C не
  додається, доки ключ не знадобиться обчислювати іншим WP (WP-01C PR2 п.6).
- **О-3 — translation jobs ставляться в транзакції `upsert_article_version`** (WP-01A PR3b;
  outbox `news.version_created` уже там): викликач (WP-05) обчислює список чистою функцією WP-04
  `plan_translation_jobs(version_meta) -> list[NewJob]` і передає його в репозиторій;
  `collector_translation` INSERT на `crawl_jobs` **не** отримує (GRANT — ролі викликача).
- **О-4 — прийнято пропозицію чернетки:** `translation.head` (title/lead, priority 300),
  `translation.body` (priority 200; не створюється без body — R-20), `translation.backfill`
  (priority 100, `args.backfill=true`); version записує той job, що бачить повний набір
  сегментів у TM, через `INSERT … ON CONFLICT DO NOTHING` за `translation_idempotency_key`.
- **О-5 → resolved рішенням користувача U-2:** мови поза основними 16 (`ru`, `ca` тощо)
  **перекладаються теж** — з тими самими gates §12.1 (preservation 100 %, `uk` ≥ 0.95
  confidence для > 200 символів) і **додатковими парами в golden corpus** PR3. Набір додаткових
  мов — конфіг `COLLECTOR_TRANSLATION_EXTRA_SOURCE_LANGUAGES` (default `ru,ca`); кожна мова з
  цього набору мусить мати теку golden corpus (тест PR3). Мова, якої немає ні серед 16, ні в
  extra, → сегмент не надсилається, quality flag `language_unsupported`, version —
  `translation_failed` з retry plan «додати мову в extra + golden pair».
- **О-6 — batch translation через GCS відкладено** (out of scope v1): у PR2 — online
  `translateText` з батчуванням сегментів у запиті; backfill іде тим самим шляхом під окремим
  бюджетом.
- **О-7 — прийнято пропозицію чернетки:** `translation_quality_result` зберігається в
  `quality_results` (WP-01A PR3b, тип `translation_rubric`); до появи API — файл-звіт
  `docs/plan/reports/<WP>/translation-qa.md`.
- **U-3 (порядок хвилі 1):** PR1 стартує одразу, паралельно з передумовами.

## Спільні вимоги для всіх PR

- Pure-логіка (segmenter, TM key, glossary, preservation, budget-політика, planner) — без I/O; I/O за портами (`Protocol`): `TranslationMemoryStore`, `BudgetLedger`, `ArticleTextSource`, `TranslationSink`, `TranslationProvider`. У unit-тестах — in-memory реалізації в `tests/unit/translation/fakes.py`.
- CPU-bound робота (парсинг HTML, детекція мови) у handler-і — через `asyncio.to_thread` (контракт `docs/workers.md` §5 п.1).
- Текст статей і сегментів **не** потрапляє в логи, `error_message`, metric labels (§13: публічні контакти/імена — лише в даних); у лог — `article_version_id`, мова, кількість сегментів/символів.
- Текст новини — дані, не інструкції (§13 «prompt-like текст»): жодного шаблонування тексту в запит, окрім поля `contents`.
- Метрики §14.1 як лічильники в коді (`translation_jobs_total{source_language,status}`, `translation_characters_total`, `translation_cost`, `translation_latency_seconds`, `translation_memory_hit_ratio`); експорт — WP-12 (патерн WP-01D PR2 п.5).
- **Тест проти мовчазного skip (урок HANDOFF §6):** `tests/unit/translation/test_no_silent_skip.py` — AST-скан усіх файлів `tests/unit/translation/**` і `tests/integration/translation/**`: заборонені `pytest.skip`, `skipif`, `importorskip`, `xfail` без `strict=True`; окремо перевіряє, що кожна параметризація по мовах/fixture-файлах має непорожній набір (порожній `parametrize` pytest тихо позначає як skip). Тест має падати на навмисно доданому `pytest.importorskip("lingua")` (доказ у звіті тестувальника).
- **Linux-паритет (урок HANDOFF §6):** unit-тести зелені в CI job `python` (Linux, `pytest-socket` блокує всі сокети); тест, що `GoogleTranslationProvider` без respx-маршруту падає швидко з помилкою мережевої політики, а не зависає і не звертається до мережі; на Windows цей тест не приймається як доказ — лише вивід CI або Linux-контейнера.
- Кожен PR ≤ ~800 рядків продуктивного коду; перевищення — розбиття на `-a/-b` того самого owner.

---

## PR1 — `wp/04-1-segmenter-tm`: segmenter, мова, TM, glossary, preservation

### Вимоги

1. **Segmenter (§10 крок 11, §16.1 п.1):** cleaned HTML → впорядкований список сегментів по блочних елементах (`p`, `li`, `h1–h6`, `blockquote`, `figcaption`, `td/th`, `dt/dd`), з поділом довгих блоків на речення лише за лімітом довжини запиту провайдера; inline-розмітка (`a`, `em`, `strong`, `b`, `i`, `br`, `span`, `sup/sub`) лишається всередині сегмента як захищені теги; `code`, `pre`, `script`, `style`, атрибути (`href`, `src`, `alt` — окремо як сегмент лише для `alt`/`title`, за конфігом) не перекладаються. `reassemble(segments, translations)` відновлює дерево. Парсер HTML — без мережі й без resolve зовнішніх entities (§13).
2. **Нормалізація для хешу (§12.2):** Unicode NFC, згортання пробілів і керівних символів; оригінальний текст не змінюється.
3. **Мова по сегментах (§5.4, §8, §12.2, R-08):** порядок — source-declared metadata → `lang` атрибут → classifier (`lingua-language-detector`, обмежений `uk` + 16 + мовами з `COLLECTOR_TRANSLATION_EXTRA_SOURCE_LANGUAGES`, default `ru,ca` — U-2/О-5); короткі сегменти (< конфігурованого порогу) успадковують мову статті; результат із confidence; невпевненість зберігається, а не ігнорується.
4. **`not_required` (§5.4):** `original_language == uk` і жоден сегмент не визначено як не-`uk` → план без запитів до провайдера, status `not_required`, текст не дублюється. Змішана сторінка: не-`uk` сегменти перекладаються, `uk`-сегменти проходять як є.
5. **TM key (FR-017) — рівно 5 компонентів:** `source_language`, `target_language`, `normalized_segment_hash` (SHA-256 нормалізованого тексту з маскованими placeholder-ами), `provider/model` (пара `provider`, `model_version`), `glossary_version`; ключ — SHA-256 canonical JSON (використати `canonical_json_bytes`/`sha256_hex` з `collector.contracts`). Порт `TranslationMemoryStore` (`get_many(keys)`, `put_many(entries)`) + in-memory реалізація; PostgreSQL-реалізація — PR2.
6. **Glossary:** versioned файл(и) у `src/collector/translation/glossary/` (терміни/імена per language pair → українська форма або do-not-translate); `glossary_version` = SHA-256 canonical вмісту; застосування через маскування до запиту і підстановку після.
7. **Маскування і preservation (§10 крок 12, §12.1):** числа (з локальними форматами `1.234,5` / `1 234,5` / `1,234.5`), дати, відсотки, валюти, URL, e-mail, терміни glossary → placeholders до запиту; після — `validate_preservation(source, translated)`: 100% placeholders на місці, множина чисел збігається після нормалізації формату, URL побайтово ті самі, теги inline-розмітки збігаються за мультимножиною. Провал → сегмент з quality flag і відмова збирати version з цим сегментом (не «тихий» пропуск).
8. **План перекладу (R-20, FR-016):** `plan_article_translation(article)` → поля до перекладу за `content_access`: `metadata_only`/`blocked`/`challenge`/`premium`/`gone` — лише наявні title/lead; `full`/`partial` — плюс body, якщо він є; `body is None` → жодного body-сегмента і жодного згенерованого тексту.
9. **Q-009:** для нової article version з частково зміненим текстом до провайдера йдуть лише сегменти без TM hit; повна version збирається з TM + нових перекладів.

### Тести (unit, без мережі)

- segmenter: для кожного fixture з `tests/fixtures/translation/html/` `reassemble(segments, identity)` дає DOM, еквівалентний вхідному (порівняння нормалізованого дерева, не рядків); вкладені `a>strong`, `li` з `br`, `blockquote>p`, таблиця — теги не рвуться і не губляться; `code/pre/script` не стають сегментами; атрибут `href` незмінний; hostile HTML (незакриті теги, `<!ENTITY>`, дуже глибока вкладеність) не падає і не виконує resolve entities.
- TM key: зміна кожного з 5 компонентів окремо (`source_language`, `target_language`, текст, `provider`, `model_version`, `glossary_version`) → інший ключ (параметризований тест, 6 випадків, бо компонент 4 — пара); зміна лише пробілів/NFC-форми → той самий ключ; golden-значення ключа зафіксовано у fixture (стабільність між процесами/ОС).
- `uk`: стаття `uk` без іншомовних сегментів → `not_required`, провайдер-fake не викликано жодного разу, текст у sink не записано.
- змішані мови: стаття `uk` з цитатою `en` і абзацом `pl` → перекладено рівно 2 сегменти з правильними `source_language`; стаття `de` з `uk`-цитатою → `uk`-сегмент не надіслано; короткий сегмент (`«Так»`) успадковує мову статті.
- body nullable (R-20): `metadata_only` з title/lead → план без body; `full` з `body=None` → без body і без помилки; жоден шлях не створює body-текст.
- preservation 100%: набір кейсів (числа в трьох локальних форматах, дати, `%`, `€`/`zł`/`Ft`, URL з query, e-mail, ім'я з glossary) — fake-провайдер, що повертає коректний результат, проходить; fake, що змінює одну цифру / губить URL / переставляє placeholder / губить `</a>` — кожен ловиться валідатором (окремий тест на кожну мутацію).
- glossary: зміна одного терміна → новий `glossary_version` → новий TM key; do-not-translate термін доходить до результату незмінним.
- Q-009: v1 з 5 абзаців перекладено; v2 змінює 1 абзац → провайдер-fake отримав рівно 1 сегмент, зібрана v2 містить 5 перекладених абзаців.
- ідемпотентність: повторний переклад тієї самої version з тим самим provider/model/glossary → 100% TM hit, 0 викликів провайдера.
- `test_no_silent_skip.py` (див. спільні вимоги).

### Acceptance PR1

Тести вище зелені локально і в CI job `python`; mypy strict; жодного I/O у pure-модулях (імпорт-тест: `collector.translation.segmenter`/`memory`/`glossary`/`preservation` не імпортують `sqlalchemy`, `httpx`, `asyncpg`); `pyproject.toml`/`uv.lock` оновлені лише власними залежностями.

---

## PR2 — `wp/04-2-provider-budget`: provider interface, Google adapter, budget, handler

### Вимоги

1. **`TranslationProvider` (Protocol):** `name`, `model_version`, `max_request_chars`, `async translate(segments, source_language, target_language, mime_type) -> list[ProviderResult]`; помилки класифіковані: `ProviderRetryable` (429/5xx/timeout, з `retry_after`), `ProviderPermanent` (400 unsupported language, невалідний запит), `ProviderAuthError` (401/403 — не ретраїться нескінченно, як §10 крок 6). Інтерфейс допускає майбутній NLLB/Marian без зміни handler-а; результати різних provider/model не змішуються (провайдер/модель — у TM key і version).
2. **`GoogleTranslationProvider`:** REST v3 `projects/{project}/locations/{location}:translateText` через `httpx.AsyncClient` (тестується respx); `mimeType=text/html` для сегментів з inline-розміткою; батчування сегментів у межах ліміту запиту; timeouts за §10 (connect 10 с / read 30 с / total 60 с); токен — через порт `AccessTokenProvider` (продакшн-реалізація на `google-auth` service account, виконується поза event loop), у тестах — fake. Credentials — лише з файлу секрету за `COLLECTOR_TRANSLATION_CREDENTIALS_FILE` (§13, FR-013); у логах/помилках — ні токена, ні вмісту ключа (перевіряється разом із `redact()` runtime-у).
3. **Budget (§15, Q-008, R-09):** два класи — `fresh` (head і body нових статей) і `backfill`; `reserve(chars, class)` перед викликом провайдера, `commit(actual)` після, `release` при помилці; рахуються символи фактично надісланого payload (консервативно, включно з розміткою). Порядок: `translation.head` → `translation.body` → `translation.backfill` через `priority` черги (`claim` сортує `priority DESC`, `repositories/queue.py`). Default: `fresh` — ліміт з конфігу; `backfill` **paused без явного ліміту** (не налаштовано → жодного виклику провайдера для backfill). Вичерпання → `TaskResult.deferred(until=початок наступного періоду, error_code="translation_budget_exhausted")` (WP-01D PR1c п.1) без спалювання спроб, status не `translation_failed`. Таблиця — `translation_budget_periods` WP-01A PR3b (О-1).
4. **`TranslationHandler`:** `job_types = ("translation.head", "translation.body", "translation.backfill")` (О-4, прийнято); читає article version через порт, будує план (PR1), бере TM hits, reserve budget, перекладає miss-сегменти, валідує preservation, пише сегменти в TM, за наявності повного набору — immutable translation version під `translation_idempotency_key` (`ON CONFLICT DO NOTHING`). `ProviderRetryable` → `TaskResult.retryable`; `ProviderPermanent`/провал preservation після повтору → `translation_failed` + quality flags + `TaskResult.permanent` (з retry plan у `error_code`); `ProviderAuthError` → permanent з кодом `provider_auth`.
5. **Provider disabled (dev/CI без credentials):** `COLLECTOR_TRANSLATION_PROVIDER=disabled` (default у compose до появи секрету) → фабрика повертає `NoopHandler(role)`, який claim-ить лише `job_type == "translation"`, тож translation jobs лишаються `pending` з незмінним `attempt`; instance `ready`, стек `up --wait` не ламається (§16.3: відсутність credentials не ламає інші компоненти).
6. **Планувальник jobs:** чиста функція `plan_translation_jobs(version_meta) -> list[NewJob]` з idempotency key на базі `translation_idempotency_key` + частина (`head`/`body`); викликач — WP-05 у транзакції `upsert_article_version(..., translation_jobs=...)` WP-01A PR3b (О-3); WP-04 не має INSERT на `crawl_jobs`.
7. **PostgreSQL-адаптери портів** (`TranslationMemoryStore`, `BudgetLedger`, `TranslationSink`) — лише через репозиторії WP-01A PR3; жодного сирого SQL у `collector.translation` (правило WP-01A «Repository API»).
8. Реєстрація фабрики в `HANDLER_FACTORIES[WorkerRole.TRANSLATION]` при імпорті `collector.translation.handler` (lazy registry WP-01D PR1c п.3); фабрика отримує `HandlerContext` (`sessions` для PostgreSQL-адаптерів портів, п.4 PR1c).
9. **Translated body як artifact (§9.1 «translated artifact refs»):** перекладений body понад поріг inline — через `collector.storage.upload` у bucket `translated` з обліковими даними `minio_translation` (WP-00 PR5); читання cleaned artifact-ів — `collector.storage.get(key, expected_sha256)`; у unit-тестах — `collector.storage.testing.FakeArtifactStore`.
10. **Мови поза 16 (U-2):** сегменти мов з `COLLECTOR_TRANSLATION_EXTRA_SOURCE_LANGUAGES` перекладаються тим самим шляхом і з тими самими gates; мова поза 16 і поза extra → `language_unsupported` + `translation_failed` з retry plan (О-5).

### Тести

- Unit, provider (respx, `pytest-socket` активний): успіх → переклад і коректний порядок сегментів; 429 з `Retry-After` → `ProviderRetryable(retry_after=…)`; 5xx/timeout → retryable; 400 → permanent; 401/403 → `ProviderAuthError`; невалідний JSON/менше перекладів, ніж сегментів → permanent, а не часткове збирання; батчування: 3 000 сегментів → кількість запитів відповідає ліміту, жоден запит не перевищує `max_request_chars`; без respx-маршруту → швидка помилка мережевої політики (Linux-паритет).
- Секрети: fixtures і тестовий код не містять `private_key`, `"type": "service_account"`, `ya29.`, `AIza` (скан `tests/fixtures/translation/**`); лог і `TaskResult.error_message` при `ProviderAuthError` не містять токена (caplog + перевірка рядка).
- Budget: ліміт 1 000 символів, 3 статті → head усіх трьох перекладено раніше за body будь-якої (порядок викликів fake-провайдера при спільній черзі); вичерпання посеред body → job deferred, `attempt` не змінився, TM зберігає вже перекладені сегменти, повтор після нового періоду добирає лише решту; backfill без налаштованого ліміту → 0 викликів провайдера, jobs deferred, а не failed; backfill із лімітом не використовує `fresh`-бюджет; паралельні `reserve` двох handler-ів не перевищують ліміт (integration на PostgreSQL за О-1 (а); за варіантом (б) — тест документує межу перевитрати).
- Ідемпотентність: повторний handle тієї самої job-и після успіху → 0 викликів провайдера (TM hit), рівно одна translation version; head і body, завершені паралельно, → рівно одна version (integration).
- `uk` → handler записує `not_required` без виклику провайдера й без reserve budget; `metadata_only` → лише head job, version без body.
- Мови поза 16 (U-2): сегмент `ru` при `EXTRA_SOURCE_LANGUAGES=ru,ca` → перекладено, preservation-gate застосовано; сегмент мови поза 16 і поза extra → 0 викликів провайдера, `language_unsupported`, `translation_failed` з retry plan.
- Preservation-провал від провайдера → `translation_failed` з quality flag, version з пошкодженим сегментом не записана.
- Provider disabled → після enqueue jobs лишаються `pending`, `attempt = 0`, instance `ready` (integration зі scaling-фікстурами WP-01D або unit на фабриці + runtime з fake queue).
- Integration (`tests/integration/translation/`, маркер `integration`, PostgreSQL 18 як у `tests/integration/postgres`): операції handler-а під **LOGIN-роллю `collector_translation`** (зразок `tests/integration/postgres/test_role_logins.py`) — може читати article version, писати TM/version/budget; не може INSERT у `crawl_jobs`, не може писати `normalized_artifacts`.
- `test_no_silent_skip.py` покриває нові файли; integration-тести в CI не skip-аються через відсутність Docker там, де job обіцяє PostgreSQL (патерн `E2E_REQUIRED` з `tests/e2e/test_gui_runtime_contract.py`).

### Acceptance PR2

Тести вище зелені локально (Linux/Docker) і в CI; передумови WP-01C PR2, WP-01A PR3a/PR3b, WP-01D PR1c, WP-00 PR5 `merged`; WP-02 PR2 `merged` — або wiring S3 (п.9) винесено в PR2b з явним записом у звіті і ledger; жодного живого виклику Google; `docker compose config --quiet` зелений з новим секретом (зміни compose — у WP-00 PR5).

---

## PR3 — `wp/04-3-golden-corpus`: golden corpus 16 мов + додаткові пари, QA-runner, rubric

### Вимоги

1. `tests/fixtures/translation/golden/<lang>/` для кожної з 16 мов `de, fr, en, lt, lv, et, pl, hu, ro, cs, sk, sl, hr, it, es, nl` **і для кожної мови з `COLLECTOR_TRANSLATION_EXTRA_SOURCE_LANGUAGES` (default `ru`, `ca` — рішення користувача U-2: ті самі gates §12.1, ті самі вимоги до кейсів і provenance)** (+ `uk` як контроль `not_required` і `mixed/` для змішаних сторінок): щонайменше 10 сегментних кейсів на мову, що покривають числа в локальному форматі мови, дати, валюти, URL, імена/посади/географію (glossary), цитати, списки, inline-розмітку, заголовок + lead + body і `metadata_only`.
2. **Provenance для кожної мовної пари `<lang> → uk`:** `provenance.yaml` у кожній теці: автор, дата створення, метод (`synthetic-authored` — авторський текст для проєкту; реальні статті не копіюються), ліцензія, мова-носій/рецензент джерельного тексту (або `unreviewed` явно), для еталонного українського перекладу — хто і як його отримав (`human-authored` / `provider-output recorded <дата> з дозволу користувача`). Відсутній або неповний `provenance.yaml` → тест падає.
3. **QA-runner** `python -m collector.translation.qa --corpus tests/fixtures/translation/golden [--provider fake|google]`: offline за замовчуванням з детермінованим fake-провайдером; метрики — preservation rate (має бути 100%), структурна рівність DOM, glossary hit, TM hit при повторі, для еталонів — regression score (chrF або інша детермінована метрика без мережі, поріг фіксується в ADR); звіт JSON + Markdown. Режим `--provider google` — лише ручний, лише з дозволу користувача і з бюджетом (§11 плану п. (г)); у CI не запускається.
4. **§12.1 перевірка української:** для перекладу > 200 символів детектор повертає `uk` з confidence ≥ 0.95 — у runner-і як gate (на fake-провайдері — на еталонних українських текстах).
5. **Rubric §12.3:** шаблон `translation_quality_result` (5 критеріїв × 0–2, середній ≥ 1.8, критерії 1 і 3 = 2/2 у кожному зразку, reviewer ID) + валідатор файлу оцінок, яким користуються WP-06x для 30 зразків/мову.

### Тести

- параметризований тест по всіх 16 мовах і додаткових мовах U-2: набір мов у тесті дорівнює константам `collector.translation.languages` (`CORE_SOURCE_LANGUAGES` — 16, `EXTRA_SOURCE_LANGUAGES` — default extra; створюються в цьому WP), і `CORE_SOURCE_LANGUAGES` порівнюється з незалежно записаним у тесті списком із §8 ТЗ, а `EXTRA_SOURCE_LANGUAGES` — з незалежно записаним `{"ru", "ca"}` (дві різні точки правди — зникнення теки не зменшує параметризацію мовчки); кожна мова має ≥ 10 кейсів; мова в extra-конфігу без теки golden → тест падає.
- кожна мовна пара: preservation 100%, DOM-структура відновлена, glossary-терміни на місці — «all language pairs green» (§17.2).
- `provenance.yaml` присутній, валідний і повний для кожної теки; текст корпусу не містить секретів.
- `uk/` → `not_required`; `mixed/` → перекладено лише не-`uk` сегменти.
- rubric-валідатор: 1.9 середній, але критерій 3 = 1 в одному зразку → fail; 30 зразків з 1.8 і 2/2 по критеріях 1, 3 → pass; менше 30 зразків для великого джерела → fail з поясненням (для малого джерела — усі записи, §12.3).
- regression: зафіксований baseline-звіт fake-прогону; зміна segmenter-а, що погіршує метрику, ловиться тестом.
- `test_no_silent_skip.py` покриває PR3.

### Acceptance PR3 (закриває WP-04)

Усі 16 пар і додаткові пари U-2 (`ru`, `ca`) зелені offline у CI; provenance повний; `docs/translation-qa.md` описує запуск runner-а, rubric і процедуру human QA; ADR Q-008/Q-009 оформлені.

## Команди перевірки (усі PR)

```bash
uv sync --frozen
uv run ruff check . && uv run ruff format --check . && uv run mypy src
uv run pytest -m "not live" -rs
uv run pytest -rs tests/unit/translation
uv run pytest -m integration -rs tests/integration/translation   # PR2+
uv run python -m collector.translation.qa --corpus tests/fixtures/translation/golden   # PR3
docker compose config --quiet                                     # PR2 (після WP-00 PR5)
```

Звіт implementer-а/tester-а містить фактичний вивід, включно з рядком підсумку pytest (`passed`/`skipped`); `skipped > 0` у файлах WP-04 — failed gate.

## Acceptance (§17.2, дослівно)

«segmenter, provider interface, Google adapter, translation memory, glossary, QA corpus; all language pairs green».

Додатково з §12.1 (translation gates, які перевіряє код WP-04): «для новин coverage translation = 100% завершених original article versions, крім записів зі статусом `translation_failed` і явним retry plan»; «placeholder/URL/числа в перекладі збережені на 100%, language detection українського результату ≥ 0.95 confidence для текстів понад 200 символів».

З §16.3 (доказ дає не WP-04, а WP-06x/WP-14; WP-04 дає інструменти): «для кожної з 19 країн щонайменше одне джерело з `source_state=enabled` віддає item із `content_access=full`, оригіналом і українським перекладом через API/SQL»; «30 випадкових перекладів на кожну вихідну мову пройшли human QA за rubric, critical meaning errors = 0».

## Відомі ризики

| Ризик | Статус | Owner | Дата |
|---|---|---|---|
| PR2 чекає на п'ять передумов (WP-01C PR2, WP-01A PR3a/PR3b, WP-01D PR1c, WP-00 PR5) і WP-02 PR2; рішення О-1/О-3/О-4 прийняті | open — стежить оркестратор за ledger | orchestrator / WP-01A | 2026-09-24 |
| Ціна і квота Google не перевірені live; credentials і бюджет — зовнішні витрати, потрібне рішення користувача (§11 плану п. (г)); до цього провайдер `disabled` | open | user / orchestrator | 2026-09-24 |
| Golden corpus синтетичний: доводить pipeline (preservation, структуру, TM, glossary), а не якість Google-перекладу; якість — лише через human QA WP-06x і контрольований live-прогін | accepted (межа offline-тестування) | WP-06x / WP-14 | 2026-09-24 |
| Розмір wheel/пам'ять `lingua-language-detector` у образі воркера (моделі 17 мов) | open — заміряти в PR1, обмежити набір мов | WP-04 | 2026-09-24 |
| Budget за О-1 (б) допускає перевитрату на один batch | closed — обрано варіант (а), таблиця з reservation (WP-01A PR3b) | orchestrator | 2026-09-24 |
| Без `TaskResult.deferred` budget-deferral спалював би спроби і слав би jobs у dead letter | закривається передумовою WP-01D PR1c п.1 + WP-01A PR3a п.1 до старту PR2 | WP-01D / WP-01A | 2026-09-24 |
| Додаткові мови U-2 (`ru`, `ca`) без human QA 30 зразків не мають доказу якості, лише offline gates | accepted — human QA для додаткових мов планує WP-06x/WP-14 за тим самим rubric | WP-06x / orchestrator | 2026-09-24 |

## Rollback/disable

`COLLECTOR_TRANSLATION_PROVIDER=disabled` → `NoopHandler`, jobs чекають у `pending` без втрати спроб; backfill вимкнений за замовчуванням (Q-008); `desired_replicas=0` для pool `translation` через `worker_pools`; оригінали не змінюються, translation versions immutable — відкат провайдера/моделі/glossary означає нову version, а не перезапис. Revert PR безпечний: схема належить WP-01A, WP-04 не має міграцій.

## Docs (етап 5)

`docs/translation-qa.md` (golden corpus, runner, rubric, human QA, як додати мову), `src/collector/translation/README.md` (сегментація, TM key, glossary, preservation, budget, як додати provider), ADR «Translation budget і пріоритети» (Q-008: класи бюджету, backfill paused, де зберігається бюджет — рішення О-1), ADR «Segment-level retranslation» (Q-009, гранулярність jobs — О-4), рядки метрик у `docs/observability/metrics.md` (для WP-12), рядок «Оригінал + переклад» у `docs/acceptance/traceability.md` (spec-reviewer).
