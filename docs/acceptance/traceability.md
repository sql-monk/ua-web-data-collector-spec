# Матриця трасування вимог (Додаток C ТЗ → докази)

Файл веде `wp-spec-reviewer` на етапі 4 кожного WP (додає рядки); WP-14 фіналізує перед acceptance report.
Один рядок — одна ціль Додатка C або її частина, покрита конкретним WP/PR. Доказ — файл, тест або звіт з фактичним виводом.
Статуси: `evidenced` / `partial` / `missing` / `not applicable`.

| Ціль Додатка C | Вимоги | Що саме покрито | Тест / файл-доказ | WP / PR | Статус | Звіт |
|---|---|---|---|---|---|---|
| Незалежна реалізація | §17, §18 | Issue template §17.3 (10 полів), PR template §17.1 (зміни, тести з виводом, fixture provenance, ризики, rollback, not-live, звіти етапів, DoD) | `.github/ISSUE_TEMPLATE/work-package.md`, `.github/pull_request_template.md` | WP-00 PR1 | evidenced | `docs/plan/reports/WP-00/spec-review-pr1.md` §1.2 п. 8 |
| Незалежна реалізація | §17, §18 | CI-контракт §16.2 (uv sync --frozen, ruff check/format, mypy src, pytest -m "not live"), gitleaks, concurrency per ref, secrets лише з GitHub | `.github/workflows/ci.yml`; `tests/unit/test_foundation_config.py::test_ci_runs_spec_16_2_commands_without_hardcoded_secrets` | WP-00 PR1 | evidenced (compose/build/SBOM кроки — PR2) | `spec-review-pr1.md` §1.2 п. 7, §1.5 |
| Незалежна реалізація | §17, §18 | Owner-стаби CLI §16.2: кожна команда → exit 2 + `not implemented: owned by WP-XX`; layout Додатка A з owner-docstring у кожному підпакеті | `src/collector/cli.py`; `src/collector/**/__init__.py`; `tests/unit/test_cli.py::test_stub_returns_exit_code_2_and_owner`; `tests/unit/test_cli_adversarial.py::test_stub_message_goes_to_stderr_only`, `::test_help_command_set_equals_spec_16_2_exactly`; `tests/unit/test_foundation_config.py::test_appendix_a_package_exists_with_owner_docstring` | WP-00 PR1 | evidenced | `spec-review-pr1.md` §1.2 п. 1, 3, 9 |
| Незалежна реалізація | §17, §18 | Відтворювана збірка: `.python-version` 3.13, `uv.lock` з hashes, `UV_FROZEN` у CI; Linux-паритет підтверджено контейнером (119 passed) | `pyproject.toml`, `uv.lock`, `.python-version`; `tests/unit/test_foundation_config.py::test_package_is_typed_and_python_pinned` | WP-00 PR1 | evidenced | `spec-review-pr1.md` «Власна верифікація» |
| Технічна безпека | FR-013, §13 | Secrets не в git: `.env*` ігноруються (`!.env.example`), жодного `.env` у `git ls-files`; gitleaks у pre-commit (staged + manual history) і CI job `secrets` (`fetch-depth: 0`) | `.gitignore`; `.pre-commit-config.yaml` (hooks `gitleaks`, `gitleaks-history`); `.github/workflows/ci.yml` job `secrets`; `tests/unit/test_foundation_config.py::test_env_files_are_gitignored_but_example_is_not` | WP-00 PR1 | evidenced | `spec-review-pr1.md` §1.1, §3 |
| Технічна безпека | FR-013, §13 | Логи приховують Authorization/Cookie/API keys/token/password/secret (case-insensitive, рекурсивно, structlog + stdlib `extra`); JSON-рядок на подію для всіх logger-ів | `src/collector/core/logging.py` (`redact_secrets`, `ProcessorFormatter`); `tests/unit/test_logging.py::test_secret_keys_are_redacted_case_insensitively`, `::test_redaction_is_recursive_in_nested_mappings_and_lists`, `::test_redaction_applies_to_stdlib_extra_fields`, `::test_stdlib_records_from_third_party_loggers_are_json` | WP-00 PR1 | evidenced (secrets); partial для контактів (R-11: ключі `phone`/`email` не в списку — owner WP-07/WP-09/WP-12) | `spec-review-pr1.md` §4 R-11, знахідка 1 |
| Технічна безпека | FR-013, §13 | CI secrets лише через `${{ secrets.* }}`; `permissions: contents: read`; жодних literal-токенів | `.github/workflows/ci.yml`; `tests/unit/test_foundation_config.py::test_ci_runs_spec_16_2_commands_without_hardcoded_secrets` | WP-00 PR1 | evidenced | `spec-review-pr1.md` §1.2 п. 7 |
| Технічна безпека | FR-013, §13 | Мережа заборонена у звичайних тестах (§8): connect до не-loopback host кидає виняток pytest-socket (sync, asyncio, httpx sync/async, urllib); integration/e2e — лише loopback; POSIX — повний блок створення socket/getaddrinfo | `pyproject.toml` addopts `--disable-socket`; `tests/conftest.py`; `tests/unit/test_network_blocked.py` (5); `tests/unit/test_network_block_adversarial.py` (6) | WP-00 PR1 | evidenced (Windows: loopback дозволений у unit — документоване відхилення; CI Linux — повний блок) | `spec-review-pr1.md` §1.2 п. 4, знахідка 4 |
| Технічна безпека | FR-013, §13 | SSRF/XXE/zip-bomb/hostile HTML tests, dependency/image scanning | — | WP-02, WP-13 | not applicable (поза PR1) | — |
| Docker і масштабування | FR-030—FR-033, §7.5—§7.6 | Ролі worker pools §7.6 (8) як enum і CLI-контракт `collector worker <role>`, `scheduler`, `controller` (стаби owner WP-01D) | `src/collector/workers/roles.py`; `src/collector/cli.py`; `tests/unit/test_cli_adversarial.py::test_worker_role_enum_matches_spec_7_6_exactly`, `::test_worker_every_spec_role_is_stub_owned_by_wp_01d` | WP-00 PR1 | partial (лише контракт ролей/CLI; images/Compose/profiles/volumes — WP-00 PR2; scale/drain/kill — WP-01D) | `spec-review-pr1.md` §3 |
| Docker і масштабування | FR-030—FR-033, §7.5—§7.6 | clean-host start, profiles, named-volume restart, non-root/read-only, pinned digests, SBOM | — | WP-00 PR2/PR3 | not applicable (PR1) | — |
| Доказовість і відтворення | FR-005, FR-006, FR-011, §9 | Shared lineage/artifact contracts: `Lineage` (fetch_id, raw_sha256, parser_version, projection_task_id), `RawArtifactRef` (HTTP metadata), `NormalizedArtifactRef` (entity/domain/parser + raw/fetch lineage), `raw_object_key = sha256(body)`, `UploadClaim` + `can_commit` fencing, fetch/translation idempotency keys, `identity_hash_v1` golden | `src/collector/contracts/{artifacts,identity,current}.py`; `tests/unit/contracts/test_artifacts.py`, `test_identity.py::test_identity_hash_golden`, `::test_fetch_and_translation_keys_golden`; `tests/fixtures/contracts/identity_golden.json` | WP-01C | evidenced (контракти; lineage integration/replay-from-raw E2E — WP-01A/01B/14) | `docs/plan/reports/WP-01C/spec-review.md` §3 |
| Часова коректність | FR-024, §9.6 | naive/non-UTC datetime відхиляється (не нормалізується); source time nullable, `fetched_at` не потрапляє у `source_event_at` (R-43); precision enum дослівно; `effective_at_basis` + `source_time_inferred`; bitemporal `[valid_from, valid_to)`/`[known_from, known_to)` для late arrival / backdated / relisting (R-49) | `src/collector/contracts/temporal.py`; `tests/unit/contracts/test_temporal.py` (17), `test_adversarial.py` розділ 3 (7); mutation M2/M3/M5 у `testing.md` §5 | WP-01C | evidenced (declared locale §9.6 — знахідка 1 low, поле відсутнє) | `spec-review.md` §1.3 вимога 3, §4 R-43/R-49, §8 |
| Оборотний matching | FR-026, §9.8 | `ResolutionDecision` з усіма полями §9.8 (5 actions дослівно), `project_groups` — детермінований replay merge → manual_block → unmerge → replay; `manual_block` до superseding decision; supersede fixed point; source records не на вході (immutable); `ResolutionSnapshot`; thresholds Q-012 не зашиті | `src/collector/contracts/resolution.py`; `tests/unit/contracts/test_resolution.py::test_merge_manual_block_unmerge_replay` (+4), `test_adversarial.py` розділ 8 (6), `test_code_review_fixes.py:135-204` | WP-01C | evidenced (контракт; resolution worker, попередній release replay — WP-07/09/11A) | `spec-review.md` §1.6, §4 R-45, §5 |
| Відтворювані дослідження | FR-027, FR-029, §9.9 | `ReleaseManifest` з усіма полями §9.9 (release_id/tag/times/owner/purpose, watermark, entity list xor partition index hash, registry/policy versions, included/excluded/degraded + reason, schema/parser/normalizer/matcher/resolution/translation versions, git commit, image digests, config hash, build command, parts, quality/reconciliation, previous/superseding); `ReleasePart` (uri, format, partition, row_count, min/max effective/system, size, sha256); `ReleaseState` 6 станів, 7 дозволених переходів; `published`/`superseded` immutable | `src/collector/contracts/release.py`; `tests/unit/contracts/test_release.py::test_manifest_has_all_spec_9_9_fields`, `::test_state_machine_transitions_table`, `::test_published_is_immutable`; `test_release_adversarial.py` (39) | WP-01C | evidenced (контракт; identical rebuild/part hashes/DuckDB — WP-11A/11B) | `spec-review.md` §1.6, §4 R-46 |
| Узгодженість двох БД | FR-020—FR-023, §7.3—§7.4, §9 | `ProjectionCommand` ≠ `DomainChangedEvent` (R-30); receipt з previous/result hash + ready event bytes/media type/SHA-256 або artifact ref (R-37/R-42), inline ≤ 256 KiB; `ProjectionAcknowledgement.from_receipt` без reserialization; `should_emit_domain_changed = applied_to_current AND state_changed` (R-36); out-of-order інваріант; `CurrentDocumentBase` = YAML §9.2 поле-за-полем (`schema_version` int); `state_hash` v1 детермінований strict-JSON | `src/collector/contracts/{projection,events,canonical,current}.py`; `tests/unit/contracts/test_projection.py` (7), `test_events.py` (10), `test_current.py` (6); mutation M1/M6; `schemas/mongo/current_document_base.v1.json` | WP-01C | evidenced (контракт; crash-window replay, reconciliation, 3-1-2 delivery — WP-01B integration) | `spec-review.md` §1.4, §1.5, §4 |
| Незалежна реалізація | §17, §18 | Єдиний owner shared contracts + dependency-процедура; 32 JSON Schema snapshots у `schemas/{common,events,mongo,releases}` з drift-check у CI (`collector contracts export --check`); compatibility fixtures (7) і `check_compatibility` (remove/rename/type/required/enum → breaking); жодного I/O в імпорті контрактів; `contracts` CLI через approved dependency `docs/plan/deps/WP-01C-to-WP-00.md` | `docs/contracts.md` §1, §3; `src/collector/contracts/schema_export.py`; `.github/workflows/ci.yml:56-57`; `tests/contract/contracts/{test_schema_snapshots,test_compatibility,test_no_io_imports,test_adversarial_export}.py` | WP-01C | evidenced (картка: owned files не містять `schemas/common/**` — знахідка 2 low) | `spec-review.md` §1.3 вимога 10, §7, §8 |
| Docker і масштабування | FR-030—FR-033, §7.5—§7.6 | Clean-host start `core`+`workers` однією командою (`up -d --wait` exit 0, 16/16 healthy/exited-0), readiness через one-shots, restart зберігає named volumes (маркери у postgres/minio) | `docker-compose.yml`; `docs/runbooks/clean-host-start.md`; `tests/unit/test_compose_config.py::test_readiness_waits_for_one_shots`; `deploy/compose/check-healthy.py` + `tests/unit/test_check_healthy.py`; CI job `docker` | WP-00 PR2 | evidenced | `spec-review-pr2.md` §1.1, «Власна верифікація» |
| Docker і масштабування | FR-030, FR-031, §7.5 | Profiles §7.5 (`core`/`workers`/`browser`; `gui`/`observability`/`tools` зарезервовані), мережі `ingress`/`backend`/`source-egress`/`provider-egress`/`telemetry`, named volumes лише stateful, Docker secrets/files | `docker-compose.yml`; `tests/unit/test_compose_config.py::test_services_match_spec_7_5_profile_table`, `::test_networks_match_spec_7_5`, `::test_service_network_placement`, `::test_named_volumes_only_for_stateful`, `::test_secrets_are_files_with_examples_and_gitignored`; `tests/integration/test_compose_render.py` (5) | WP-00 PR2 | partial (`gui` — PR3; `observability` — WP-12; `tools` — WP-11A/WP-14; `api` у `ingress` замість «лише OIDC egress» — accepted, owner WP-00 PR3/WP-11A, 2026-09-22) | `spec-review-pr2.md` §1.3 |
| Docker і масштабування | FR-030, FR-031, §7.6 | Один multi-stage image `collector` (pinned base tag+digest, non-root 10001, read-only rootfs + tmpfs, OCI labels Git SHA/schema, HEALTHCHECK); default replicas §7.6 (discovery 1, fetch 2, browser 0, parse 2, projector 1, translation 1, export 1, maintenance 1); `--scale fetch-worker=4→1` без зміни image | `Dockerfile`; `docker-compose.yml`; `tests/unit/test_compose_config.py::test_worker_is_scalable` (SPEC_7_6_REPLICAS), `::test_dockerfile_is_multistage_pinned_non_root_without_secrets`, `::test_scheduler_is_singleton` | WP-00 PR2 | partial (concurrency/`worker_pools`/drain/lease і origin rate — WP-01D/WP-02; app image без immutable registry digest) | `spec-review-pr2.md` §1.2, §1.4, знахідка F-3 |
| Технічна безпека | FR-013, FR-035, §13 | Жодного Docker socket у 16 контейнерах; non-root/read-only/`cap_drop: ALL`/`no-new-privileges`/`pids` для application services і MinIO; секрети лише Docker secrets/files, відсутні в image (`docker history`, `env`) і в git | `docker-compose.yml`; `deploy/compose/secrets/init-secrets.sh`; `tests/unit/test_compose_config.py::test_no_docker_socket_mount_anywhere`, `::test_application_services_are_read_only_non_root_with_tmpfs`, `::test_every_service_has_pids_limit_and_log_rotation`, `::test_minio_is_capability_dropped_and_read_only`, `::test_services_reference_only_declared_secrets_and_no_secret_env`; `tests/integration/test_compose_render.py::test_no_docker_socket_and_no_published_ports` | WP-00 PR2 | evidenced (MinIO uid 0 — датований risk acceptance ADR-0002, owner WP-01D) | `spec-review-pr2.md` §1.5, §4 (R-55) |
| Технічна безпека | FR-013, §13 | Dependency/image scanning: SBOM (syft, SPDX artifact) і trivy (CRITICAL блокує без `ignore-unfixed`; HIGH з fix блокує) на кожен PR; датований risk acceptance для 2 unfixed HIGH (CVE-2026-82560 perl, CVE-2026-85091 zlib) | `.github/workflows/ci.yml` job `docker`; `docs/decisions/0002-docker-compose-single-host.md` (risk acceptance, owner WP-13, 2026-09-22, тригери); `tests/unit/test_compose_config.py::test_ci_trivy_critical_without_ignore_unfixed_and_high_with` | WP-00 PR2 | partial (job `docker` ще не виконувався — `operationally unverified`; щотижневого розкладу сканів немає — знахідка F-2, owner WP-13) | `spec-review-pr2.md` §1.2 п. 9c/9d, §1.5 |
| Незалежна реалізація | §17, §18 | Q-006/Q-013 safe default зафіксовано ADR-0002 (Context/Decision/Consequences/Date/Owner/Status); стаб-межі власників (WP-01A migrate, WP-01B validators, WP-01D worker/scheduler/lease, WP-11A api, WP-02 browser image, WP-12 observability) закріплені в коді, compose і ADR | `docs/decisions/0002-docker-compose-single-host.md`; `src/collector/cli.py`; `src/collector/api/health.py`; `docker-compose.yml` | WP-00 PR2 | evidenced | `spec-review-pr2.md` §5, §7 |
| Керований збір | FR-001, FR-002, FR-004, §3 | Реєстр джерел як дані (PG): `sources`/`source_policy_versions`/`source_routes`/`source_cursors` зі станами `SourceState`/`RouteState`, лімітами (rps, concurrency, burst, crawl interval), `robots_policy`, `manifest_sha256`, circuit breaker і optimistic `revision` — зміна без коду ядра (FR-001); FR-002 — `start_run` + unique partial `crawl_runs(source_id) WHERE status='running' AND kind='full'`; FR-004 (частина «per-origin rate limit») — canonical PG token bucket + concurrency permits | `src/collector/persistence/postgres/models/control.py`, `repositories/sources.py`, `repositories/crawl_runs.py:100`, `repositories/limiter.py`; `tests/integration/postgres/test_control_plane.py` (4), `test_limiter.py` (9), `test_adversarial.py::test_parallel_start_full_run_grants_exactly_one` | WP-01A PR1 | partial (storage/state — evidenced; розклад, robots, conditional GET — WP-02/WP-03) | `docs/plan/reports/WP-01A/spec-review-pr1.md` §1.3, §3 |
| Docker і масштабування | FR-030—FR-033, §7.5—§7.6 | PG-частина worker pools і лімітера: `worker_pools` (desired + min/max/mode/revision), `worker_instances` (boot UUID, 5 статусів §7.6, slots/leases, heartbeat, `drain_requested_at`), `scale_commands` (idempotency key, expected/applied revision, 7 станів `requested→draining→awaiting_manual_apply\|applying→applied\|failed\|superseded`, `cli_command` для Compose, audit link) — desired-state update і insert команди в одній транзакції; current виводиться з heartbeat (`observed_capacity`), `applied` дозволений лише при збігу; FR-033/R-53 — глобальний token bucket + concurrency permits за normalized origin, rate і concurrency окремо, release/expiry ідемпотентні, `block_origin` без burst після 429 | `src/collector/persistence/postgres/models/pools.py`, `repositories/pools.py`, `models/limiter.py`, `repositories/limiter.py`; `tests/integration/postgres/test_pools.py` (11), `test_limiter.py` (9), `test_adversarial.py` (10 pools/limiter), `tests/unit/persistence/postgres/test_policies.py::test_scale_command_transition_table_matches_card` | WP-01A PR1 | partial (PG-контракти — evidenced; replicas `1→4→1→0→2`, drain/kill, concurrency hot-change — WP-01D, §16.1 п.15) | `docs/plan/reports/WP-01A/spec-review-pr1.md` §1.3, §4 (R-53) |
| Технічна безпека | FR-013, §13 | Облікові дані БД розділені за компонентами: 8 NOLOGIN group-ролей (`collector_migrate`, `scheduler`, `fetcher`, `parser`, `projector`, `translation`, `api_ro`, `export_ro`) з пооб'єктними GRANT; migration role не використовується runtime-кодом; `audit_log` append-only тригером **і** відсутністю UPDATE/DELETE grant; DSN лише з env або secret-файла, у логах завжди redacted | `src/collector/persistence/postgres/sql/roles.sql`, `roles.py`, `config.py`; `tests/integration/postgres/test_roles.py` (4), `test_role_connections.py` (17 — окремі login-з'єднання, матриця `has_table_privilege` по 13 таблицях, `::test_migrate_role_is_not_referenced_by_runtime_code`), `test_cli_db.py::test_db_migrate_never_prints_password_even_when_dsn_has_one` | WP-01A PR1 | evidenced (для §13 «облікові дані БД розділені за компонентами» і «migration role не в runtime»; SSRF/XXE/scans — WP-02/WP-13) | `docs/plan/reports/WP-01A/spec-review-pr1.md` §3, §1.1 С-9 |
| Експлуатація | FR-009, §14 | pause/resume/disable як стан джерела (`sources.state` + reason/actor + optimistic revision); dead letters як таблиця з reason/attempt/error і полями resolution + keyset-перегляд (§15, `OFFSET` не використовується ніде) | `src/collector/persistence/postgres/repositories/sources.py:89`, `models/queue.py:147-167`, `repositories/queue.py:333-350`; `tests/integration/postgres/test_control_plane.py::test_source_state_uses_optimistic_revision`, `test_queue.py::test_retry_backoff_then_max_attempts_quarantines_with_dead_letter` | WP-01A PR1 | partial (стан і сховище — evidenced; 60-секундний pause SLO, bounded backfill, dead-letter replay, dashboards/alerts — WP-02/WP-11A/WP-12) | `docs/plan/reports/WP-01A/spec-review-pr1.md` §1.7, §3 |
| Незалежна реалізація | §17, §18 | Єдине ownership PostgreSQL-міграцій: 3 forward-only Alembic-ревізії з `upgrade`/`downgrade`, `alembic check` без drift на чистій БД (мутація M0 доводить чутливість), CHECK-и генеруються з shared-enum і звіряються з живою БД; CI job `integration-postgres` із pinned digest `postgres:18@sha256:86c951e0…`; зміни у файлах WP-00 — лише через dependency-запит | `migrations/postgres/**`, `alembic.ini`, `migrations/postgres/env.py`; `.github/workflows/ci.yml:65-115`; `tests/integration/postgres/test_migrations.py` (11), `test_schema_contract.py` (15), `test_adversarial.py::test_upgrade_head_twice_in_a_row_is_idempotent`; `docs/plan/deps/WP-01A-to-WP-00.md` | WP-01A PR1 | partial (усе локально зелене; job `integration-postgres` ще жодного разу не виконувався — гілка не push-иться до merge-gate) | `docs/plan/reports/WP-01A/spec-review-pr1.md` §1.6, §2 |
| Docker і масштабування | FR-030—FR-033, §7.5—§7.6 | **Closing acceptance усього WP-00**: clean-host старт `core`+`workers`+`gui` однією командою §16.2 (`up -d --wait` exit 0, усі 17 контейнерів healthy / one-shots exited 0) — відтворено пострев'ю незалежно | `docker-compose.yml` (сервіс `gui`); `docs/runbooks/clean-host-start.md`; `README.md:99`; `.github/workflows/ci.yml` job `docker` (`COMPOSE_PROFILES: core,workers,gui`) | WP-00 PR3 | evidenced | `spec-review-pr3.md` §1.1, «Власна верифікація» |
| Docker і масштабування | FR-030, FR-031, §7.5 | Profile `gui` став реальним; мережі §7.5 доповнено internal-мережею `frontend` (gui↔api), `api` прибрано з `ingress` — публічний порт має єдиний сервіс `gui`; gui не має доступу ні до `backend`, ні до workers | `docker-compose.yml` (networks `frontend`/`ingress`, `gui.networks`, `api.networks`); `tests/unit/test_compose_config.py` (`test_gui_container_is_non_root_read_only_without_secrets`, network-інваріанти); `tests/integration/test_compose_render.py` | WP-00 PR3 | evidenced (`observability` — WP-12; `tools` — WP-11A/WP-14) | `spec-review-pr3.md` §1.2 вимога 4a |
| Docker і масштабування | FR-030, §7.5, §8 | Другий multi-stage image `collector-gui` (node builder → `nginxinc/nginx-unprivileged`, обидва base pinned tag+digest), non-root 101:101, read-only rootfs + tmpfs, OCI labels (revision = Git SHA), HEALTHCHECK; web lock `package-lock.json` + `npm ci` у CI | `web/Dockerfile`; `web/package-lock.json`; `docker-compose.yml` (build context + `additional_contexts: gui-conf`); `.github/workflows/ci.yml` job `web`, крок `docker compose build gui` з перевіркою label `revision` | WP-00 PR3 | evidenced | `spec-review-pr3.md` §1.2 вимоги 1/4, §1.3 |
| Технічна безпека | FR-013, §13 | Restrictive CSP (`default-src 'none'`, `script-src/connect-src 'self'`, `base-uri/form-action/frame-ancestors/object-src 'none'`) і security headers (`nosniff`, `DENY`, `no-referrer`, Permissions-Policy, COOP/COEP/CORP, HSTS) на КОЖНОМУ типі location; `server_tokens off`; детальний `/api/v1/health/components` назовні не виходить (auth_request → ready/not_ready) | `deploy/compose/gui/nginx.conf`; `tests/e2e/test_gui_runtime_contract.py` (12 runtime-тестів: headers, CSP, SPA fallback, internal location, path normalization); `tests/e2e/test_gui_api_down_branch.py` (3) | WP-00 PR3 | evidenced (перевірено пострев'ю на живому стеку: `curl -I http://localhost/`, `pytest -m e2e` 25 passed) | `spec-review-pr3.md` §1.2, §4.1 |
| Технічна безпека | FR-013, §13 | Токени не в `localStorage`/`sessionStorage`: ESLint `no-restricted-globals`/`-properties`/`-syntax` (+`indexedDB`/`caches`) з посиланням на §13, runtime-guard `browserStorageGuard`, E2E-перевірка сховища повз сторінку | `web/eslint.config.js:91-121`; `web/src/browserStorageGuard.ts`; `web/tests/unit/{eslint-no-browser-storage,browser-storage-guard}.test.ts`; `web/tests/e2e/smoke.spec.ts` | WP-00 PR3 | evidenced (межі guard-а задокументовані: same-origin iframe, IndexedDB/Cache API, активний XSS — не покриті; проти зловмисника працюють CSP і відсутність токена в JS) | `spec-review-pr3.md` §1.2 вимога 3 |
| Технічна безпека | FR-013, §13 | Host header injection закрито (`proxy_set_header Host $api_host`, `proxy_redirect`, `absolute_redirect off`); spoofable `X-Forwarded-For` замінено на `$remote_addr`; query string не потрапляє в access-log (`log_format gui_no_query`); source maps не збираються у образ і віддають 404 | `deploy/compose/gui/nginx.conf`; `web/vite.config.ts` (`sourcemap: mode !== 'image'`); `tests/unit/test_compose_config.py` (`test_gui_nginx_does_not_forward_client_host_to_api`, `..._does_not_forward_spoofable_client_ip`, `..._access_log_drops_query_string`, `..._does_not_serve_source_maps`) | WP-00 PR3 | evidenced (перевірено пострев'ю: forged Host → `location: /api/v1/health/components`; у образі 0 файлів `*.map`) | `spec-review-pr3.md` §4.1 |
| Технічна безпека | FR-013, §13 | Dependency/image scanning для web-шару: `npm audit --omit=dev --audit-level=high` + повний `npm audit --audit-level=high` (обидва блокують), SBOM (syft SPDX) і два trivy-скани для `collector-gui` з тією самою політикою, що для `collector` | `.github/workflows/ci.yml` job `web` (крок npm audit), job `docker` (SBOM GUI, trivy GUI CRITICAL/HIGH); `tests/unit/test_compose_config.py::test_ci_web_job_audits_npm_dependencies`, `::test_ci_generates_sbom_for_both_images`, `::test_ci_trivy_critical_without_ignore_unfixed_and_high_with` | WP-00 PR3 | partial (`npm audit` — 0 vulnerabilities; локальний скан образу `collector-gui` виконано за S-4 через `docker scout` і виявив 13 CRITICAL / 41 HIGH зі застарілих apk-пакетів базового образу — закрито `apk upgrade` у `web/Dockerfile`, після якого лишився 1 HIGH без фіксу upstream; самі кроки syft/trivy у CI — `operationally unverified`) | `spec-review-pr3.md` §3, S-4; `implementation-pr3.md` «Локальний скан образу `collector-gui`» |
| Operator GUI | FR-034—FR-037, §7.7, §9.10 | Каркас: React 19 + TypeScript strict + Vite, TanStack Query + React Router, route-level code splitting (окремі chunk-и на маршрут), українська placeholder-сторінка, дев'ять екранів §7.7 як контракт у коді, заготовка generated OpenAPI client з інваріантами (same-origin, cookie-сесія, CSRF, cache key з версією схеми, cursor pagination, SSE окремо) | `web/src/routes/{router.tsx,OverviewPage.tsx,screens.ts}`; `web/src/api/{README.md,queryClient.ts,schemaVersion.ts}`; `web/tests/unit/{routes,build-contract}.test.tsx?`; `web/playwright.config.ts` + `web/tests/e2e/smoke.spec.ts` | WP-00 PR3 | partial (навмисно: екрани, generated client, cursor tables, SSE, RBAC-gating, a11y і operator flows §7.7 — WP-11C; OIDC BFF/CSRF/RBAC — WP-11A) | `spec-review-pr3.md` §3, §4 (R-54) |
| Незалежна реалізація | §17, §18 | Web-частина CI-контракту §16.2 як job `web` (`npm ci` → audit → lint → test → build → test:build → Playwright e2e); runtime-тести `gui` виконуються між `up -d --wait` і `down -v` у job `docker` | `.github/workflows/ci.yml` jobs `web`/`docker`; `tests/unit/test_compose_config.py::test_ci_runs_web_pipeline_from_spec_16_2`, `::test_ci_runs_gui_runtime_tests_against_live_stack` | WP-00 PR3 | evidenced (S-1 закрито: гейт переведено на власну `COLLECTOR_E2E_REQUIRED`, яку виставляє лише крок job `docker` після `up -d --wait`; runtime-модулі gui виключені з job `python` по шляху, щоб не звужувати команду §16.2 і не приховати майбутні offline-тести WP-14; обидві гілки відтворені локально) | `spec-review-pr3.md` S-1; `implementation-pr3.md` «Відповіді на пострев'ю» |
| Docker і масштабування | FR-030—FR-033, §7.5—§7.6 | Runtime worker pools: `WorkerRuntime` (boot UUIDv7 → `worker_instances` `starting` → readiness → `ready`, heartbeat за дедлайном із бюджетом на тік), claim-loop до `desired_concurrency` через репозиторій WP-01A, `TaskHandler`/`NoopHandler`, продовження lease у транзакції heartbeat, `complete/retry/quarantine`; defaults §7.6 для всіх 8 ролей (`parse` = 2 × CPU контейнера, `browser` 0 × 1) | `src/collector/workers/{runtime,handlers,config,roles}.py`; `src/collector/cli.py` (`worker`, `scheduler`); `tests/integration/scaling/test_worker_runtime.py::test_instance_registers_becomes_ready_and_stops_on_drain`, `::test_bootstraps_missing_pool_from_spec_defaults`, `::test_handler_failure_becomes_a_retry_with_backoff`; `tests/unit/workers/` (5 модулів); рантайм: 16 контейнерів healthy, `worker_instances` за defaults §7.6 | WP-01D PR1 | evidenced (runtime/pool/instance контракти; origin limiter — PR2; `scale_commands`/adapters — PR3) | `docs/plan/reports/WP-01D/spec-review-pr1.md` §1.1, §2.1, §3 |
| Docker і масштабування | FR-031, §7.5, §15 | Stateless worker: `worker_instance_id` генерується на boot, жодного стану на локальному диску, Docker hostname/`container_id` — лише metadata `worker_instances`; replacement replica підхоплює expired lease після readiness | `src/collector/workers/runtime.py:137`, `config.py::from_env`; `tests/unit/workers/test_roles_and_state.py::test_workers_package_writes_nothing_to_local_disk`; `tests/integration/scaling/test_worker_runtime_adversarial.py::test_second_boot_of_the_same_container_creates_a_new_instance`; `test_worker_runtime.py::test_killed_replica_lease_is_recovered_and_finished_by_another_instance`; Docker: `kill -9` → exit 137 → `stale` → рестарт того самого контейнера дає новий `instance_id` | WP-01D PR1 | evidenced | `docs/plan/reports/WP-01D/spec-review-pr1.md` §2.1 вимоги 3/5, §3 |
| Docker і масштабування | FR-032, §7.5, §7.6, §16.3 | SIGTERM → `draining` (claim зупинено, активні дотягуються в `stop_grace_period`, lease повертаються без backoff) → exit 0; SIGKILL — fault case; гаряча зміна `desired_concurrency` без рестарту (нові слоти одразу, зайві після завершення активних, стеля процесу clamp-иться); per-instance drain barrier (`worker_instances.drain_requested_at`) як примітив R-57 | `src/collector/workers/{runtime,signals}.py`; `tests/integration/scaling/test_worker_runtime.py::test_drain_finishes_active_task_and_takes_no_new_jobs`, `::test_drain_timeout_returns_the_lease_to_the_queue`, `::test_concurrency_hot_change_opens_and_closes_slots_without_restart`, `::test_hot_change_above_the_connection_ceiling_is_clamped`, `::test_role_wide_drain_barrier_stops_claim_without_sigterm`; `test_worker_runtime_adversarial.py::test_drain_barrier_must_be_set_on_every_instance_of_the_role`, `::test_drain_timeout_on_the_last_attempt_never_quarantines_a_job_nobody_failed`; Docker: `stop` → `draining` → `stopped` → exit 0 за 1.8 с | WP-01D PR1 | partial (drain/kill/hot-change — evidenced; **role-wide** барʼєр і scale-down replicas — PR3 `PoolController`, чесно названо per-instance у коді, картці й звітах; job на останній спробі повертається після експірації lease — потрібен `queue.release`, owner WP-01A PR2) | `docs/plan/reports/WP-01D/spec-review-pr1.md` §1.2, §2.1 вимоги 3/4, §4 (R-52, R-57) |
| Docker і масштабування | FR-030, §7.5 | Self-fencing lease: сторож окремою задачею рахує час від останнього **підтвердженого базою** heartbeat і скасовує активні tasks, якщо база не підтверджує lease довше за `fence_after` (½ TTL) — працює і на зависанні PostgreSQL без винятку; `command_timeout` asyncpg + `statement_timeout` у транзакції heartbeat дають нижню межу на рівні драйвера | `src/collector/workers/runtime.py` (`_watchdog_loop`, `_fence_if_lease_unconfirmed`), `config.py` (`fence_after`, `heartbeat_tick_budget`, `command_timeout`), `src/collector/persistence/postgres/engine.py` (`command_timeout`); `tests/integration/scaling/test_worker_runtime.py::test_self_fencing_fires_when_the_database_hangs_without_raising` (сесія зависає без винятку), `::test_self_fencing_cancels_active_tasks_when_the_database_stops_confirming_the_lease` | WP-01D PR1 | evidenced (з ремаркою: `statement_timeout` ставиться лише в heartbeat, `command_timeout` — лише на CLI-шляху; знахідка S-4) | `docs/plan/reports/WP-01D/spec-review-pr1.md` §6.1 (H-1), §8 S-4 |
| Docker і масштабування | FR-030, §7.5 | Singleton scheduler через PostgreSQL session advisory lease: другий instance не стає активним і чекає; володіння перевіряється на боці сервера (`pg_locks` + `pg_backend_pid()`), втрата lease негайно зупиняє планування; maintenance tick (`recover_expired_leases` + `mark_stale_instances`) ідемпотентний; нових таблиць/колонок не потрібно | `src/collector/workers/{advisory,scheduler}.py`; `tests/integration/scaling/test_scheduler_singleton.py` (5, зокрема `::test_maintenance_tick_is_safe_when_two_schedulers_overlap`), `test_scheduler_singleton_adversarial.py` (4); Docker: 2 контейнери scheduler → рівно один `scheduler.activated` | WP-01D PR1 | evidenced (залишковий TOCTOU між перевіркою lease і commit-ом тіку закритий **контрактом** «тік ідемпотентний» + вартовим-тестом, не конструкцією — знахідка S-5) | `docs/plan/reports/WP-01D/spec-review-pr1.md` §2.1 вимога 6, §6.1 (M-4) |
| Технічна безпека | FR-013, §13 | **Свідоме тимчасове відхилення:** 8 worker-сервісів і `scheduler` монтують той самий secret `postgres_dsn`, що й `migrate-postgres` (роль `collector`, `rolsuper = t`) — §13 «migration role не використовується runtime-процесами» порушено, бо всі `collector_*` ролі NOLOGIN і LOGIN-користувачів ще немає. Компенсації: жодних інших credentials (Mongo/MinIO), `api` без секретів, Docker socket відсутній, rootfs read-only, non-root; оформлено dependency-запитом, рядком «Відомі ризики» картки (owner WP-01A PR2, 2026-09-23) і тест-вартовим | `docker-compose.yml` (`x-worker`, `scheduler`); `docs/plan/deps/WP-01D-to-WP-01A.md` §2; `tests/unit/test_compose_config.py::test_runtime_dsn_is_a_temporary_deviation_from_13_with_a_tripwire`, `::test_postgres_dsn_secret_is_scoped_to_migration_and_queue_consumers`; `tests/unit/test_compose_config_adversarial.py::test_api_has_no_secrets_and_runtime_has_only_the_dsn`; рантайм-доказ `docs/plan/reports/WP-01D/testing-pr1.md` §6 F1 | WP-01D PR1 | partial — порушення §13 відкрите (блокер pilot/production і live-збору, не блокер merge); тест-вартовий ловить `CREATE ROLE … LOGIN`/`WITH LOGIN`, але мовчить на lowercase `login` і на `CREATE USER … PASSWORD` і бачить лише `roles.sql` (знахідка S-2) | `docs/plan/reports/WP-01D/spec-review-pr1.md` §3, §6.2, §6.3, §8 S-2 |
| Docker і масштабування | FR-030, §7.5 | **Дешевий liveness-probe** для Docker healthcheck довгоживучого worker-процесу (без запуску нового інтерпретатора і без звертання до БД), щоб повернути `x-healthcheck-budget` до секундних значень | — (healthcheck лишається `python -m collector.api.health <deps>`; у звітах PR1 вимоги немає ні як виконаної, ні як перенесеної) | WP-01D PR1 | **missing** — вимога 7 картки PR1; додана на `main` (WP-00 PR3) після merge-base гілки, тож зʼявиться при ребейзі; закривати реалізацією або явним перенесенням з owner/датою | `docs/plan/reports/WP-01D/spec-review-pr1.md` §2.1 вимога 7, §8 S-1 |
| Незалежна реалізація | §17, §18 | Findings рецензентів PR1 позначені і перевірені в коді: gate 2 F0 `not applicable`, F1 `accepted` (WP-01A PR2, 2026-09-23), F2–F6 `fixed`; gate 3 H-1 і M-1…M-3 `fixed` (сторож lease окремою задачею, retry переходу в `ready`, стеля concurrency + точний pool зʼєднань), M-4 `fixed контрактом + вартовим`, L-1…L-4/L-6 `fixed`, L-5 і частина L-7 `accepted` з owner/датою | `docs/plan/reports/WP-01D/{implementation,testing,code-review}-pr1.md`; перевірка кожного `fixed` по коду — `spec-review-pr1.md` §6.1 | WP-01D PR1 | evidenced (DoD §18 п. 8) | `docs/plan/reports/WP-01D/spec-review-pr1.md` §6.1, §7 |
| Історія без втрат/дублів | FR-005, FR-006, FR-011, R-38/R-41, R-36 | Upload claim fencing (unique object key, `claim_generation` monotonic, `commit_reference` лише з предикатом owner+generation+lease live); `record_parse_result` ідемпотентний за `parse_key` (ідентичність parse-кроку, ADR-0007 D-2), не за вмістом artifact — A→B→A дає 3 версії; outbox `fetch_unpublished` — доставка щонайменше один раз через visibility lease, паркування після `max_attempts` | `src/collector/persistence/postgres/repositories/{artifacts,projection,outbox}.py`; `tests/integration/postgres/test_upload_claims.py` (11), `test_projection.py::test_parallel_record_parse_result_issues_versions_without_gaps_or_duplicates`, `::test_repeated_record_for_same_artifact_returns_same_task`; `code-review-pr2-r2.md` CR-1..CR-3 закрито | WP-01A PR2 | evidenced (з умовою: коректність `parse_key` залежить від дисципліни WP-02 щодо `fetch_id`/ключа normalized artifact — `docs/plan/deps/WP-01A-to-WP-02.md`) | `docs/plan/reports/WP-01A/spec-review-pr2.md` §1.1 (P-9..P-12), §5.2 (D-2) |
| Узгодженість двох БД | FR-020—FR-023, §7.3—§7.4, §9 | `acknowledge_projection` в одній транзакції: `projection_acknowledgements` PK `task_id` (повторний ack ідемпотентний), `entity_index.confirmed_projection_version = GREATEST(existing, receipt.version)` (ніколи не зменшується — доставка 3,1,2 лишає confirmed=3), `change_events`/`outbox_events(domain.changed)` лише при `applied_to_current AND state_changed`, bytes побайтово з receipt без reserialization; crash-вікно між кроками ack не лишає часткових рядків | `src/collector/persistence/postgres/repositories/projection.py:505-630`; `migrations/postgres/versions/20260923_0005_entity_version_guard.py` (тригер-guard); `tests/integration/postgres/test_projection.py` (ack-тести T-3/T-4), `test_pr2_adversarial.py` (crash-вікно) | WP-01A PR2 | evidenced (PG-частина; Mongo-сторона й повний replay — WP-01B) | `docs/plan/reports/WP-01A/spec-review-pr2.md` §1.1 (P-14), §1.6 (Z-2), §1.7 (H-1..H-3) |
| Доказовість і відтворення | FR-005, FR-006, FR-011, §9 | Lineage переживає видалення raw object: `raw_sha256`/`raw_uri`/`fetch_id`/`parser_version` — колонки без FK у `fetches`, `parse_attempts`, `normalized_artifacts`; `record_parse_result` вимагає узгодженості lineage між `parse_attempt` і `artifact_ref` (SR-2, закрито) | `src/collector/persistence/postgres/models/{artifacts,projection}.py`; `repositories/projection.py::_require_consistent_lineage`; `tests/integration/postgres/test_projection.py::test_attempt_and_artifact_lineage_must_describe_one_parse` (5 кейсів) | WP-01A PR2 | evidenced (PG-контракт; повний E2E replay-from-raw — WP-01B/WP-14) | `docs/plan/reports/WP-01A/spec-review-pr2.md` §9 SR-2; `docs/plan/reports/WP-01A/implementation-pr2.md` «Fixes after gate 4» |
| Технічна безпека | FR-013, §13 | LOGIN-ролі per component (`collector_scheduler/fetcher/parser/projector/translation/api_ro/export_ro`), пароль — SCRAM-SHA-256 verifier з DSN-секрету (`collector db roles --with-login`, `roles.apply_logins`), `verify_runtime_login` відмовляє superuser/`collector_migrate`-членам і власним `CREATEDB`/`REPLICATION`; column-level GRANT (напр. `collector_projector` не може переписати `parse_key`/`projection_version`/`artifact_id`, N-4) і Row Level Security на `outbox_events` (parser бачить/пише лише `topic='internal'`) | `src/collector/persistence/postgres/{roles.py,sql/roles.sql}`; `tests/integration/postgres/test_role_logins.py` (17: реальні LOGIN-з'єднання, `test_projector_cannot_rewrite_task_identity_columns`, `test_gate4_privileged_memberships_are_refused[...]`) | WP-01A PR2 | evidenced для схеми ролей і GRANT; **open** — runtime-сервіси (WP-01D) досі ходять під superuser DSN `postgres_dsn` до per-role secrets WP-00 (`docs/plan/deps/WP-01A-to-WP-00.md` §4, `WP-01A-to-WP-01D.md`) — блокер pilot, не блокер merge PR2 | `docs/plan/reports/WP-01A/spec-review-pr2.md` §1.1 (P-2..P-5), §8, §9 SR-1/SR-6 |
| Експлуатація | FR-009, §14 | Сигнали відставання maintenance без падіння запису: DEFAULT-партиція `fetches_default` приймає рядки поза межами створених місячних партицій, метрика `default_partition_row_count`; outbox backlog/вік найстаршого неопублікованого — `count_backlog`, `oldest_unpublished_age(topics=(DOMAIN_TOPIC,))` (узгоджено з `count_backlog` щодо `parked`, N-3); паркування після `max_attempts` доставки (`list_parked`/`unpark`, CR-3) | `src/collector/persistence/postgres/{partitions.py,repositories/outbox.py}`; `tests/integration/postgres/test_fetch_partitions.py`, `test_outbox_entities.py::test_oldest_unpublished_age_matches_backlog_scope` | WP-01A PR2 | evidenced (сховище/сигнал; публікація в дашборд/алерт §14.2 і DELETE-based purge — WP-12) | `docs/plan/reports/WP-01A/spec-review-pr2.md` §1.2 (T-5), §9 N-2/N-3 |
| Незалежна реалізація | §17, §18 | Findings трьох рев'ю PR2 позначені й перевірені: gate 3 CR-1..CR-10/S-1..S-6 `fixed` (`code-review-pr2-r2.md`); gate 4 SR-1 (gitleaks) `fixed` (`.gitleaksignore` fingerprint, значення будується в рантаймі), SR-2 (lineage) `fixed`, SR-5 (docstring) `fixed`, N-1..N-5 `fixed`; D-1 (партиціювання)/D-2 (`parse_key`) прийняті з ADR-0007 і правкою ТЗ §9.1 | `docs/plan/reports/WP-01A/implementation-pr2.md` «Fixes after gate 4»; `docs/decisions/0007-event-tables-global-unique-over-partitioning.md` | WP-01A PR2 | evidenced (DoD §18 п.8). Gate 4' accept @ `321515f`: SR-1/SR-2/SR-3/SR-5, N-3..N-5 fixed; SR-4, SR-6, N-1, N-2 transferred з owner; SR-7 fixed (картка), SR-8 → пункт PR3. CI — operationally unverified до відкриття PR | `docs/plan/reports/WP-01A/spec-review-pr2.md` §9; `docs/plan/reports/WP-01A/spec-review-pr2-r2.md`; `docs/plan/reports/WP-01A/implementation-pr2.md` |
| Технічна безпека | FR-013, §13 | Per-component DSN-секрети: `init-secrets.sh` генерує 7 × `postgres_dsn_<component>` (user `collector_<c>`, власний 192-бітний hex-пароль, ідемпотентно, атомарний запис + lock, перевірка генератора); `migrate-postgres` = `collector db migrate && exec collector db roles --with-login`, монтує `postgres_dsn` + 7 DSN → рівно 7 LOGIN-ролей; паролі не потрапляють в `environment`, `docker inspect` чи логи | `deploy/compose/secrets/init-secrets.sh:53,153-165`; `docker-compose.yml:339-366,695-712`; `tests/unit/test_secrets_role_dsn.py` (`:87,97,110,208,233,250`), `tests/unit/test_secrets_role_dsn_adversarial.py` (`:115,178,253,301`); CI-крок `.github/workflows/ci.yml:404-455`, виконаний локально (`testing-pr4.md:60-95`) | WP-00 PR4 | evidenced (Linux CI ще не запускався — умова злиття SR-2; runtime-сервіси на своїх DSN — WP-01D PR1b, pilot-блокер закривається лише парою PR) | `docs/plan/reports/WP-00/spec-review-pr4.md` §1.1–1.2 |
| Технічна безпека | FR-013, §13 | REVOKE PUBLIC при першому initdb: на БД застосунку (`current_database()`) `REVOKE CONNECT, TEMPORARY FROM PUBLIC` + `GRANT CONNECT` 8 group-ролям §13; на `postgres`/`template1` `REVOKE ALL FROM PUBLIC`; вартовий init-SQL посилено (лише `GRANT CONNECT ON DATABASE`, без `TO PUBLIC`/паролів/LOGIN/`*.sh`) | `deploy/compose/postgres/init/02-revoke-public.sql:21-35`; `tests/unit/test_compose_config.py::test_postgres_init_revokes_public_on_app_and_service_databases`, `::test_postgres_init_scripts_are_mounted_read_only_for_wp_01a`; Docker: `temp:false`, порожній PUBLIC ACL, `collector_fetcher` → `permission denied for database "postgres"` (`testing-pr4.md:81-86`) | WP-00 PR4 | evidenced для нових кластерів; для наявних — accepted L-2 (WP-00 / оператор, 2026-09-24), ручний рецепт у `deploy/compose/postgres/init/README.md` | `spec-review-pr4.md` §1.1 A4, §6 |
| Docker і масштабування | FR-030, §7.5 | Clean-host start не зламано 7 новими обов'язковими file-secrets: CI і локальний запуск генерують їх одним скриптом; хости до PR4 отримують лише бракуючі; каталоги-заглушки Docker Desktop прибираються; повторний `up` ідемпотентний (секрети побайтово незмінні) | `.github/workflows/ci.yml:266`; `tests/unit/test_secrets_role_dsn.py:250,294,311,322`; `docs/runbooks/clean-host-start.md`; `testing-pr4.md:60-95` (`up --wait` core+workers exit 0, повторний `up` exit 0, `sha256sum -c` OK) | WP-00 PR4 | partial (локально evidenced; Linux CI з `gui` — умова злиття SR-2) | `spec-review-pr4.md` §1.3–1.4, §4 R-51 |
| Незалежна реалізація | §17, §18 | Findings PR4: gate 2 F-1..F-3 fixed, gate 3 CR-1..CR-3/L-1 fixed з тестами, L-2 accepted з owner/датою; лише owned-файли, forbidden (`src/**`, worker/scheduler-сервіси, тест-вартовий WP-01D) не зачеплено; gitleaks `main..HEAD` 13 комітів чистий | `implementation-pr4.md` «Fixes after gate 2/3»; `git diff main...HEAD --stat` | WP-00 PR4 | partial (F-4, security I-1..I-4 без owner/date — SR-1; CI/merge SHA — SR-2) | `spec-review-pr4.md` §2, §6 |
